# Codex Skills (Local Only)
These are operator playbooks for deterministic edits.
- PS5.1 only unless explicitly stated
- AllSigned compatible
- Parse gate, PS5.1 gate, ScheduledTaskAction gate, then re-sign with A33
- Never touch Firewall/Policy (treat as immutable)
